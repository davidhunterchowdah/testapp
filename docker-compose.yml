version: "3.9"

services:
  connectivity-tester:
    build: .
    container_name: python-connectivity-tester
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - PORT=5000
    restart: unless-stopped
